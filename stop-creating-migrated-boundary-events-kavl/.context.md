Remove compact_ref() call from migration.rs ensure_schema().
Migration should only upgrade the tip tree and update
.schema-version — no Migrated/Compacted commit.

Also strip Migrated events in merge_event_streams() for
backward compat with any Migrated events already in the wild.

Key files: migration.rs (ensure_schema, compact_ref),
mod.rs (merge_event_streams).
