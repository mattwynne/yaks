# Goal

Decide how to migrate existing yaks whose stored state is \ if blocked becomes derived availability rather than workflow state.

# Questions

- Do existing blocked yaks become \ plus an unresolved external blocker?
- Do we need an explicit external/manual blocker concept?
- How should old event logs be interpreted?
- Can this be backward-compatible in projections?
