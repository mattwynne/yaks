# Goal
Update pi extensions after the package rename from `@mariozechner/*` to `@earendil-works/*` and the TypeBox import change.

# Problem
pi 0.74.0 loads from `@earendil-works/pi-coding-agent`. Existing project/global extensions import old package names like `@mariozechner/pi-coding-agent`, `@mariozechner/pi-ai`, `@mariozechner/pi-tui`, and sometimes `@sinclair/typebox`, causing extension load failures.

# Definition of Done
- Project extensions in `.pi/extensions` import documented packages:
  - `@earendil-works/pi-coding-agent`
  - `@earendil-works/pi-ai`
  - `@earendil-works/pi-tui`
  - `@earendil-works/pi-agent-core`
  - `typebox`
- Relevant TypeScript/package tests still pass where available.
- Commit project changes.
