See plan: `yx field --show git-in-browser persistence plan.md` (task 2)

Minimal Vite app with isomorphic-git + lightning-fs (IndexedDB). No React, no WASM, no Disco — just a page with console output. Focus on git operations and observability.
