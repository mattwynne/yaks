See plan: `yx field --show git-in-browser persistence plan.md` (task 4)

Create a blob, build a new tree, create a commit with parent = current notes ref tip, update the ref locally. Verify with normal git tooling that the commit shape matches what yaks expects.

Depends on: prove read path (must pass).
