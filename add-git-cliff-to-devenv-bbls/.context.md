# Status

Closed as superseded.

The implemented release flow does not use `git-cliff`. The repo now uses a maintained `CHANGELOG.md` plus helper scripts (`bin/changelog-section`, `bin/check-release`, `bin/release`) instead.

If we later want automated changelog generation from commits, create a new yak for that design.