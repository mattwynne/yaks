# Goal
Show the yak's stable ID in `yx show` output.

# Status
Still valid and ready to JFDI.

# Definition of Done
- Pretty `yx show <yak>` includes the yak ID in an unobtrusive place.
- JSON `yx show --format json` already includes the ID and should keep doing so.
- Tests cover the pretty output.
