# Status

Still unresolved after release work landed.

Current observation: `Cargo.toml` is `0.2.0`, but `flake.nix` still contains `version = "0.1.0"` for both the Rust package and release zip derivation.

Decision needed:

- Should `bin/release VERSION` update `flake.nix` too?
- Or should `flake.nix` derive the version from Cargo metadata / avoid hardcoding it?
- Or is the Nix package version intentionally independent?

Definition of done: pick one policy and make release validation enforce it if consistency matters.