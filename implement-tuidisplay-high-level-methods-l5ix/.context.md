# Goal
TuiDisplay implements the new high-level methods, composing full ratatui
frames instead of piecemeal Viewport::Inline renders.

# Definition of Done
- TuiDisplay implements all 4 new DisplayPort methods
- show_yak renders a complete ratatui frame (header box + sections)
- message() works for TUI context
- No more fallback delegation to ConsoleDisplay for converted methods
- cargo test passes

# Known Knowns
- File: src/adapters/tui_display/mod.rs
- display_header_box is already converted to ratatui
- Can compose single frame with Block, Paragraph, Layout widgets

# Known Unknowns
- How much of show_list/show_log to convert vs fallback for now
- Whether TuiDisplay should delegate to ConsoleDisplay for unimplemented views initially
