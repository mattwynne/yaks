# Goal
Clean up: remove deprecated low-level methods from DisplayPort trait
and all adapter implementations.

# Definition of Done
- DisplayPort trait has only: show_yak, show_list, show_log, message, width
- All 13 old methods removed from trait
- ConsoleDisplay old method implementations removed (helpers kept if reused internally)
- TuiDisplay old method implementations removed
- No compiler warnings about dead code
- cargo test passes, Cucumber tests pass

# Known Knowns
- This is the final cleanup step — only safe after all use cases migrated
- Some private helper functions in ConsoleDisplay may still be used by new methods
- Default panicking impls added in step 2 make this safe — if any old call remains, it panics (caught in tests)
