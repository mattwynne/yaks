# sync

sync is currently unreliable.

we need to work out what to do if we try to merge a ref
with a different schema in it.

We may also want to get more clever about merging changes now that events are serialized properly.