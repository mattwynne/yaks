Sync seems to be working nicely, via git.

Need to get more people to test it out.

It would be sweet if we could configure another origin to allow the same yak map to be shared across multiple repos.

We could also look at automerge (https://automerge.org/) for an alternative/compliment to git.

There's some ux polish we could do to the output when sync runs, especially around being transparent when we resolve merge conflicts.
