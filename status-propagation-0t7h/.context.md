Add automatic status propagation rules to the yaks
domain, matching Disco discovery tree conventions:

Upward (child → parent):
- Any child stuck → parent becomes stuck
- Any child wip → parent becomes wip

Downward (parent → children):
- Parent removed → children removed (already works)

These rules run in YakMap when state changes are applied.

Prerequisite: "add stuck state" must land first.
Ships independently on main via TDD.
