# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# yaks data directory
.yaks

# verification marker for git hooks
.last-checked
.last-checked.json
.last-checked.json.tmp

# Claude Code
!.claude/
.claude/settings.local.json
.claude/worktrees/

# worktrees
.worktrees

# nix build artifacts
result
result-linux

# ralph loop prompt
prompt.md

# pre-commit config (personal/local, generated from template)
.pre-commit-config.yaml

# Rust build artifacts
target/

# cargo-mutants output
mutants.out/
mutants.out.old/

# pi agent config (allow prompts/agents/extensions, ignore the rest)
.pi/*
!.pi/.gitignore
!.pi/prompts/
!.pi/agents/
!.pi/extensions/
node_modules/
