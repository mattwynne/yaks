# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# yaks data directory
.yaks

# verification marker for git hooks
.last-checked

# Claude Code (ignore local settings only)
.claude/settings.local.json

# worktrees
.worktrees

# nix build artifacts
result
result-linux

# ralph loop prompt
prompt.md

# pre-commit config (personal/local, generated from template)
.pre-commit-config.yaml

# Rust build artifacts
target/

# cargo-mutants output
mutants.out/
