# Goal
Replace 13 low-level display methods with 4 high-level ones that accept view models.

# Definition of Done
- DisplayPort trait has: show_yak, show_list, show_log, message, width
- Old methods kept temporarily with default panicking impls (for incremental migration)
- src/domain/ports/user_display.rs updated
- cargo check passes (adapters get default impls, no breakage yet)

# Known Knowns
- File: src/domain/ports/user_display.rs
- New methods: show_yak(&YakDetailView), show_list(&YakTreeView), show_log(&[LogEntryView]), message(&Message), width() -> usize
- Old methods get default impl { panic!("deprecated") } so existing adapters still compile
- This is a transitional step — old methods removed after all use cases migrate

# Known Unknowns
- Whether width() should stay on the trait or move elsewhere
