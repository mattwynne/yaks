# Goal
Make the protect-main error message more actionable.

# Problem
The current message says "Use `dev` commands to create a worktree first" but:
- `dev` may not be on PATH — should mention `bin/dev start <yak-name>` as fallback
- No mention of read-only bash commands (ls, grep, find, which) being blocked

# Change
Update the BLOCK_REASON in .pi/extensions/protect-main/rules.ts to say something like:

  🛑 Blocked: you're in the main repo, not a worktree.
  To start work on a yak: `bin/dev start <yak-name>`
  Then use a subagent with cwd set to the worktree path.
  Only `read`, `yx *`, and `bin/dev *` commands are allowed here.
