# Goal

Improve blocker feature executable documentation by replacing abstract role-placeholder yak names with vivid, domainful examples where that improves readability.

# Scope

This is wording/test-data cleanup only. Do not change behaviour.

Do not revisit the cycle/error-message modelling work here; `unify blocker domain model and events` already changed user-facing cycle wording to `circular dependency`. Preserve that behaviour.

# Problem

`features/blockers.feature` uses many abstract names such as:

- `blocked yak`
- `blocking yak`
- `a`, `b`, `c`
- `parent`, `child`, `grandchild`

These are precise for some graph-shape examples, but overuse makes the feature less useful as executable documentation.

# Guidance

Use vivid, domainful examples where they communicate intent better, especially for happy-path and user-facing readiness scenarios. Examples:

- `deploy release`
- `security review`
- `legal approval`
- `publish announcement`
- `waiting for credentials`
- `book venue`
- `sponsor approval`

Keep abstract names where abstraction genuinely makes the graph shape clearer, especially simple cycle-shape cases using `a`, `b`, `c`.

# Acceptance / Test Approach

Start by reviewing `features/blockers.feature` and any directly related step expectations.

Update scenario data and expected output strings only. Run the blocker feature tests to confirm behaviour is unchanged.

# Definition of Done

- Happy-path blocker examples use vivid names where clearer.
- Abstract names remain only where they help explain graph shape.
- No production behaviour changes.
- Relevant Cucumber tests pass.
- Commit changes with a clean message and no AI attribution.
