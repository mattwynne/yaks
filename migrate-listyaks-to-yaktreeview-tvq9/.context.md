# Goal
ListYaks builds a YakTreeView and calls display.show_list() once.

# Definition of Done
- ListYaks::execute builds YakTreeView from tree structure
- Single call to app.display.show_list(&view)
- JSON branch removed (JsonDisplay adapter handles it)
- Format mode (pretty/markdown/plain/ids) carried in YakTreeView or passed to adapter
- node_to_json_value helper removed
- Cucumber tests pass unchanged

# Known Knowns
- File: src/application/list_yaks.rs
- Complex tree traversal logic builds YakNode tree
- Tree traversal stays in use case; rendering moves to adapter
- ids format outputs bare IDs (one per line)

# Known Unknowns
- Whether format mode belongs in view model or is an adapter configuration
- How to handle the ids format (separate method? view model variant?)
