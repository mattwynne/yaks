# Implementation Plan

## Files
- Modify: `src/main.rs` — add `--tag` arg to List command, pass to ListYaks
- Modify: `src/application/list_yaks.rs` — filter yaks before tree building, add "ids" format
- Create: `features/filter_by_tag.feature` — Cucumber scenarios

## Design
Dead simple: filter the flat `yaks` vec early in execute(), before build_tree.
- Parse --tag value: strip @ prefix using normalize_tag
- Filter: `yaks.retain(|y| y.tags.contains(&tag))`
- Add "ids" format: just print yak IDs one per line (before tree building, like json)

## Tasks
1. Add --tag arg to CLI and ListYaks, filter yaks vec before tree building
2. Add --format ids output (flat list of yak IDs)
3. Write Cucumber scenarios
4. Verify all tests pass
