# Goal
CLI selects ConsoleDisplay or JsonDisplay based on --format flag,
instead of passing format string into use cases.

# Definition of Done
- --format json → JsonDisplay injected as DisplayPort
- --format pretty/markdown/plain/ids → ConsoleDisplay injected (with format config)
- --tui → TuiDisplay (existing behavior unchanged)
- Use cases no longer receive format parameter
- ShowYak and ListYaks command structs simplified (no format field)

# Known Knowns
- CLI wiring likely in main.rs or a CLI adapter module
- Format flag currently passed through to use case constructors
- Need to find where App is constructed and DisplayPort is injected

# Known Unknowns
- Exact file where CLI wiring happens
- Whether format affects commands other than show/ls
