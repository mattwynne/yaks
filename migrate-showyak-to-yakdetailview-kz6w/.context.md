# Goal
ShowYak use case builds a YakDetailView and calls display.show_yak() once,
instead of orchestrating 10+ low-level display calls.

# Definition of Done
- ShowYak::execute builds YakDetailView from yak data
- Single call to app.display.show_yak(&view)
- JSON branch removed (handled by JsonDisplay adapter selection)
- Children sorted by done state then name (business logic stays in use case)
- All existing show_yak unit tests pass (may need adjustment for new API)
- Cucumber tests pass unchanged

# Known Knowns
- File: src/application/show_yak.rs
- Breadcrumb: walk parent chain to build Vec<String>
- Tags formatted with format_tag() helper
- Context and fields come from yak aggregate

# Known Unknowns
- Whether format_tag() output belongs in view model or adapter
