See plan: `yx field --show git-in-browser persistence plan.md` (task 3)

Fetch/clone the fixture repo. Can we fetch just refs/notes/yaks, or do we need a full clone? Resolve the ref, read the commit, walk the tree, read blob contents. Compare against known fixture data. Record which APIs work.

KILL SWITCH: if we cannot read the notes ref tree at all, stop here and skip to "write up findings".
