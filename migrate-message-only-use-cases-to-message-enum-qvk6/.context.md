# Goal
Replace display.success/info/hint/warn calls with display.message(&Message::*)
across ~10 use cases.

# Definition of Done
- All use cases calling display.success/info/hint/warn use display.message() instead
- Files: add_tag.rs, start_yak.rs, remove_yak.rs, prune_yaks.rs, compact_events.rs,
  add_yak.rs, show_context.rs, show_field.rs, generate_completions.rs, reset_git_from_disk.rs
- Mechanical replacement, output unchanged
- cargo test passes

# Known Knowns
- Simple find-and-replace pattern
- display.success("x") → display.message(&Message::Success("x".into()))
- display.warn("x") → display.message(&Message::Warn("x".into()))
- etc.
