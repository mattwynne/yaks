Move the Cli struct (clap derive), Commands enum, styles(), and related types from main.rs into an adapter module in src/lib.rs. This keeps clap as an infrastructure detail behind the adapter boundary, removes clap imports from main.rs, and enables use cases like ShowHelp to access Cli::command() for help rendering.
