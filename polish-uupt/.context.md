# Status

Remaining polish after the release work landed.

Known remaining items:

- Add dry-run support to `bin/release`, if useful.
- Decide whether to add a version consistency hook/check outside the release flow.

Note: release-time validation already exists in `bin/check-release VERSION`, but it does not resolve the `flake.nix` version mismatch and is not currently a general pre-commit/pre-push hook.