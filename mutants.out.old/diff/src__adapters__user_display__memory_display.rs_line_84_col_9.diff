--- src/adapters/user_display/memory_display.rs
+++ replace <impl DisplayPort for InMemoryDisplay>::log_entry with ()
@@ -76,24 +76,17 @@
         let indent = "  ".repeat(depth);
         self.info_messages
             .write()
             .unwrap()
             .push(format!("{indent}- [{state}] {name}"));
     }
 
     fn log_entry(&self, author_name: &str, author_email: &str, timestamp: &str, message: &str) {
-        self.info_messages
-            .write()
-            .unwrap()
-            .push(format!("{} <{}>  {}", author_name, author_email, timestamp));
-        self.info_messages
-            .write()
-            .unwrap()
-            .push(message.to_string());
+        () /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[cfg(test)]
 mod tests {
     use super::*;
     use crate::domain::slug::Name;
 
