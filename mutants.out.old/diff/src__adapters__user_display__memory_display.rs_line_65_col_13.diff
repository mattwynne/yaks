--- src/adapters/user_display/memory_display.rs
+++ delete match arm "wip" in <impl DisplayPort for InMemoryDisplay>::display_yak_pretty
@@ -57,17 +57,17 @@
         self.info_messages
             .write()
             .unwrap()
             .push(message.to_string());
     }
 
     fn display_yak_pretty(&self, prefix: &str, name: &Name, state: &str) {
         let indicator = match state {
-            "wip" => "●",
+             /* ~ changed by cargo-mutants ~ */
             "done" => "●",
             _ => "○",
         };
         self.info_messages
             .write()
             .unwrap()
             .push(format!("{prefix}{indicator} {name}"));
     }
