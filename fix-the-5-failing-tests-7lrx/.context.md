# Fix the 5 failing tests

Fix whichever side is wrong — the tests or the implementation — for each of
these 5 failures:

1. `git_checks.feature` — "YX_ROOT must point to a git repo": error message
   doesn't match expected `"YX_ROOT does not point to a git repository"`
2. Directory naming — "Directory is named by slug, not ID": `my-cool-yak` dir missing
3. Directory naming — "Special characters are stripped": `clean-up-tests-docs` dir missing
4. Directory naming — "Nested yak stored under parent slug": `my-project/fix-the-build` missing
5. `reset.feature` — "Reset creates slug-based directory with name and id files":
   `.name` file missing after reset

Investigate whether the tests are ahead of the implementation (features written
but not yet built) or the implementation regressed.
