# Context

Follow-up from `guard starting non ready yaks`.

We preserved existing `yx start --recursive` bulk behavior while adding the non-recursive readiness guard, because recursive semantics need explicit design.

Questions to answer before changing behavior:

- Should recursive start require the root yak to be ready?
- Should each descendant be individually ready?
- Should explicit blockers be bypassed by recursive start?
- Should hierarchy blockers be bypassed by recursive start?
- Should `yx start --recursive` remain a bulk state command, or be removed/renamed?
- Add BDD coverage for the chosen semantics before changing behavior.
