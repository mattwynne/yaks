# Goal
Create the Claude Code plugin marketplace manifest and plugin scaffold.

# Files to create
- `.claude-plugin/marketplace.json` — marketplace catalog pointing at the yx plugin
- `plugins/yx/.claude-plugin/plugin.json` — plugin manifest (name, description, version)
- `plugins/yx/skills/` — empty skills directories: `map/`, `prepare/`, `work/`

# Marketplace manifest
```json
{
  "name": "yaks",
  "owner": {
    "name": "Matt Wynne"
  },
  "metadata": {
    "description": "Skills for using yaks (yx) — DAG-based TODO list CLI",
    "pluginRoot": "./plugins"
  },
  "plugins": [
    {
      "name": "yx",
      "source": "./plugins/yx",
      "description": "Skills for mapping, preparing, and working on yaks"
    }
  ]
}
```

# Plugin manifest
```json
{
  "name": "yx",
  "description": "Skills for mapping, preparing, and working on yaks",
  "version": "0.1.0"
}
```

# Definition of Done
- Both JSON files are valid and correctly structured
- Empty skill directories exist with placeholder SKILL.md files
- Commit the result
