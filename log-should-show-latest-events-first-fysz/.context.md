# Goal
`yx log` currently shows events oldest-first. It should show latest-first
(like `git log`), so the most recent activity is at the top.

# Files
- Modify: src/application/show_log.rs — reverse the event order before display
- Modify: features/log.feature — update the "Events appear oldest-first" rule
  to expect newest-first instead

# Approach
Simple: reverse the events vec before iterating, or iterate in reverse.
The events come from get_all_events() in chronological order (oldest first).

# Definition of Done
- `yx log` shows newest events first
- Feature test updated to expect newest-first order
- All tests pass: dev check
