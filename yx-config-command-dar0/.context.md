Add a `yx config` subcommand for user-level configuration stored in ~/.config/yaks/.

Minimal initial surface:
  yx config set <key> <value>
  yx config get <key>

First use case: `yx config set plugin-hint false` to suppress the Claude plugin installation banner.

Config file format TBD (TOML? JSON? simple key=value?).