# Status

Superseded / completed elsewhere.

Explicit blocker relationships were implemented under:

- `separate workflow state from blocked availability`
  - `replace blocked state with blocked_by relationship`

Do not implement from this legacy yak. Use the current blocker commands/model instead:

- `yx blocker add <yak> --by <blocker> [--reason ...]`
- `yx blocker add <yak> --reason ...` for manual/external blockers

This yak can be marked done as bookkeeping; any future non-blocker sibling relationship work belongs under `sibling relationships/options/alternatives` or a newly designed yak.
