When CLAUDECODE=1 and the yx plugin is not installed, show a banner on every yx invocation:

  💡 Install the yx plugin for better Claude integration:
     claude plugin marketplace add mattwynne/yaks && claude plugin install yx@yaks
     To suppress this message: yx config set plugin-hint false

Detection: check ~/.claude/plugins/installed_plugins.json for "yx@yaks" (fast file read, no subprocess).

Suppression: config stored in ~/.config/yaks/ (needs yx config command — separate yak).

Banner shows on stderr so it does not interfere with yx output.