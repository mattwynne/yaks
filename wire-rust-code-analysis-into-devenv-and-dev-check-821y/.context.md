## Goal
Add `rust-code-analysis` nix package to `devenv.nix` and add a metrics step to `dev check` that runs it and reports results.

## What needs to happen
1. Add `rust-code-analysis` to the `packages` list in `devenv.nix`
2. Add a step in the `check()` function in `bin/dev` that runs `rust-code-analysis-cli --metrics` on Rust source files
3. For now, just report metrics and fail if the tool itself errors — threshold enforcement comes in a separate yak

## Key files
- `devenv.nix` — add package
- `bin/dev` — add metrics step to `check()`

## Acceptance criteria
- `rust-code-analysis-cli` is available in the dev shell
- `dev check` runs metrics and displays results
- No threshold enforcement yet — just visibility
