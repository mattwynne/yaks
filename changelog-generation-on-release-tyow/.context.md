# Status

Done via `bin/release VERSION`.

The release script promotes `## [Unreleased]` to `## [VERSION] - DATE` in `CHANGELOG.md`. Release notes are extracted later by `bin/changelog-section VERSION` for the GitHub release workflow.

This is maintained-changelog promotion, not `git-cliff` generation.