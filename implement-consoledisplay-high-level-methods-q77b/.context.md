# Goal
ConsoleDisplay implements show_yak, show_list, show_log, message methods
producing identical output to today.

# Definition of Done
- ConsoleDisplay implements all 4 new DisplayPort methods
- show_yak output matches current yx show pretty format exactly
- show_list output matches current yx ls pretty/markdown/plain/ids formats exactly
- show_log output matches current yx log format exactly
- message() handles hint/success/info/warn with correct stdout/stderr routing
- Existing Cucumber tests pass unchanged
- Old methods remain (still called by un-migrated use cases)

# Known Knowns
- File: src/adapters/user_display/mod.rs
- Reuse existing private helpers (style_yak_item, write_box_line, etc.)
- show_list needs a format mode (pretty/markdown/plain/ids) — from YakTreeView or constructor
- warn goes to stderr, others to stdout

# Known Unknowns
- How to pass format mode for list (constructor param vs view model field)
- Whether show_yak needs the display_header_box extraction or inline
