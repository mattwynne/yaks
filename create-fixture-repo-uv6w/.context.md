See plan: `yx field --show git-in-browser persistence plan.md` (task 1)

Create a tiny test repo with 2-3 yaks and known blob contents on refs/notes/yaks, pushed to a disposable GitHub remote. This gives verifiable test data for every subsequent step.
