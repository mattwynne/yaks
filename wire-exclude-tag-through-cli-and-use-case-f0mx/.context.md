# Goal
Wire the --exclude-tag CLI option through PruneYaks use case to
the domain prune() call.

# Files
- Modify: src/main.rs — add `exclude_tag: Option<String>` to
  `Commands::Prune`, pass to `PruneYaks::new()`
- Modify: src/application/prune_yaks.rs — accept optional tag in
  constructor, pass to `yak_map.prune()`
- Modify: tests/features/test_world.rs — update `prune_yaks` trait
  to accept `Option<&str>`
- Modify: tests/features/in_process_world.rs — pass tag through
- Modify: tests/features/full_stack_world.rs — pass `--exclude-tag`
  flag to CLI when tag is Some
- Modify: tests/features/steps.rs — new step for "I prune done yaks
  excluding tag {string}"

# Approach
1. Add Gherkin step `When I prune done yaks excluding tag "keep"`
   that calls the new world method
2. Update TestWorld trait: `prune_yaks(&mut self, exclude_tag: Option<&str>)`
3. Update both world implementations
4. Add `--exclude-tag` to Commands::Prune in main.rs
5. Update PruneYaks to accept and forward the tag
6. Update route_command to pass the option through

# Definition of Done
- All prune scenarios pass (old and new)
- `yx prune --help` shows `--exclude-tag` option
- `dev check` passes
