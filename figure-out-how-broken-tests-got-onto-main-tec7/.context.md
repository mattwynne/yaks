# Figure out how broken tests got onto main

`dev check` runs cucumber tests before merging to main. These 5 tests are
failing now, so either:

- They were already failing when merged (dev check wasn't run or was bypassed)
- They passed at merge time but something changed since (environment, dependency)
- The merge workflow has a gap (e.g. dev check runs a subset of tests)

Investigate commit c26343b3d5bd2fa65f45ff5e6603e35d556fe8a5 and the commits
around it to understand how this happened, and recommend a fix to prevent
recurrence.
