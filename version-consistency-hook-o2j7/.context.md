# Status

Still todo / needs decision.

Release-time validation exists in `bin/check-release VERSION`, but there is not yet a general hook/check that enforces version consistency during normal development.

Also note the current mismatch: `Cargo.toml` is `0.2.0` while `flake.nix` contains `version = "0.1.0"`. Decide whether this check should include `flake.nix` or whether Nix versioning should be handled differently.