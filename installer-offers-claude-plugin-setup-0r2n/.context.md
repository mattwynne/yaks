After installing the yx binary, if `claude` is on PATH, offer to install the Claude Code plugin:

  "Would you like to set up Claude Code integration? This adds skills so Claude knows how to use yaks."

If yes, run:
  claude plugin marketplace add mattwynne/yaks
  claude plugin install yx@yaks

Same pattern as the existing shell completion offer — detect, offer, skip if not applicable.

File: install.sh