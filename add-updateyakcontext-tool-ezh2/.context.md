Add an update_yak_context tool to the show-yak extension in .pi/extensions/show-yak/index.ts. The tool should accept a yak name and markdown content, then pipe the content to `yx context <name>` to update the yak's context. This avoids the pain of constructing bash pipes manually.
