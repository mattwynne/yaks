# Status

Done.

Implemented through:

- `.github/workflows/release.yml` for stable `vX.Y.Z` tag-triggered releases.
- `.github/workflows/edge.yml` for mutable prerelease builds from `main`.
- `install.sh` support for latest stable, explicit `YX_VERSION`, and `YX_CHANNEL=edge`.