We have a mix of unit tests and full-stack acceptance tests.

I'd like to separate them, so we have the acceptance tests all in one folder like `spec/features`
and the rest in `spec/unit`. If possible, the spec/unit tests will test a function directly.
