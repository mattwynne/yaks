# Goal
Thread the git commit SHA through EventMetadata so it's available for display.

# Files
- Modify: src/domain/event_metadata.rs — add `commit_sha: Option<String>` to EventMetadata
- Modify: src/adapters/event_store/git.rs — populate commit_sha from commit.id() in get_all_events()

# Approach
- Add field with default None (memory store doesn't have SHAs)
- In git.rs get_all_events(), set commit_sha = Some(commit.id().to_string()) when building metadata
- Abbreviate to 7 chars at display time, not storage time

# Definition of Done
- EventMetadata has commit_sha field
- Git event store populates it
- Unit test confirms SHA is present in events from git store
- All tests pass: dev check
