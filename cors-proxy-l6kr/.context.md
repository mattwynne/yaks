Stateless CORS proxy for isomorphic-git to talk to
GitHub git smart protocol from the browser.

~20 lines. Cloudflare Worker or Vercel edge function.
No state, no auth logic, just adds CORS headers.

The spike used cors.isomorphic-git.org (public, not
reliable for production). We need our own.
