# Status

Superseded for blocker work.

Explicit blocker relationships have been implemented under:

- `separate workflow state from blocked availability`
  - `replace blocked state with blocked_by relationship`

Do not implement `blocked_by` from this yak.

# Remaining Purpose

Keep this yak only for future non-blocker sibling relationship ideas, such as options/alternatives.

The remaining child `options/alternatives` is still valid as a separate design topic. The old `blocked_by` child is superseded by the completed blocker work.
