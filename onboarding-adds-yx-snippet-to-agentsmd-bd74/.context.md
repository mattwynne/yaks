During onboarding (after gitignore setup and tutorial offer), offer to add a yx snippet to AGENTS.md (or CLAUDE.md if the user prefers).

This gives any AI tool — not just Claude with the plugin — baseline awareness of yx:

  "Would you like me to add yx instructions to AGENTS.md so AI tools know about your yaks?" [Y/n]

Snippet should be minimal — just enough for an agent to know yx exists and how to check current work. Something like:

  ## Work tracking
  This project uses yaks (yx) for work tracking.
  Run `yx ls` to see current work.
  Run `yx show <name>` for details on a yak.

Should detect if AGENTS.md already exists and append, or create it.
Should also offer to commit the change.