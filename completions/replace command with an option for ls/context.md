The completions sub-command is just a list of all the yaks, just in a different format.

So we should move over to using `yx ls --format plain|raw` for the completions scripts. We can make the current `ls` output be the default, --format markdown|md
