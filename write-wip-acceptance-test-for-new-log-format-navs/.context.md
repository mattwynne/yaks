# Goal
Write a failing @wip acceptance test in features/log.feature that specifies
the new narrative log format. This test guides the rest of the implementation.

# Files
- Modify: features/log.feature — add new @wip rule with scenario
- Modify: features/compact.feature — update log assertions for summary line (also @wip)

# New scenario shape
```gherkin
@wip
Rule: Log entries use narrative format

  Example: Log shows narrative with author, timestamp and SHA
    Given I have a clean git repository
    And I add the yak "Fix the Bug"
    When I run yx log
    Then it should succeed
    And the output should include "added Fix the Bug"
    And the output should include "sha:"
    And the output should include "event:"
    And the output should include "────"
```

# Depends on
- CUCUMBER_WIP mode (child yak, already being implemented by subagent)

# Definition of Done
- @wip scenario exists and fails (expected — implementation comes later)
- Can be run with: CUCUMBER_WIP=1 cargo test --test cucumber --features test-support
- Existing tests still pass: dev check
