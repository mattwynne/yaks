# Goal
Add a helper that formats event timestamps as relative ("3 hours ago")
or absolute, using a library. Lean on library defaults — no custom cutoffs.

# Files
- Modify: Cargo.toml — add chrono-humanize or similar timeago crate
- Create or modify: src/adapters/user_display/mod.rs or a small helper module
  for the formatting function

# Approach
- Find a crate that works with chrono (already a dependency)
- chrono-humanize is a good candidate
- Simple function: takes epoch seconds, returns human string
- Unit test it returns something sensible

# Definition of Done
- Relative timestamp helper exists and works
- Unit test confirms output
- All tests pass: dev check
