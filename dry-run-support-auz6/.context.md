# Status

Still todo.

`bin/release VERSION` currently performs the real release prep, commit, tag, and push. Add a dry-run mode if we want to preview version/changelog changes and validation without committing, tagging, or pushing.