# Goal
Wire everything together: new narrative format, timestamps, SHAs into yx log output.

# Depends on (child yaks)
- add commit_sha to EventMetadata
- add format_narrative to EventFormat trait  
- add relative timestamp formatting

# Files
- Modify: src/domain/ports/user_display.rs — update log_entry signature
- Modify: src/adapters/user_display/mod.rs — implement 3-line format:
  Line 1: narrative (yak names in bold/colour)
  Line 2: relative timestamp (dim)
  Line 3: event: <id>  sha: <short_sha> (dim)
  Followed by: ──────── horizontal rule
- Modify: src/application/show_log.rs — build narrative, format timestamp, pass sha
  Compacted: single summary line instead of expanded snapshots
- Update unit tests in show_log.rs for new format

# No-colour mode
Same 3 lines + rule, just without ANSI codes. Already handled by
the existing color flag in display options.

# Definition of Done
- yx log outputs new narrative format
- @wip acceptance test passes (then remove @wip tag)
- Update existing log.feature assertions for new format (remove old git-log style rule)
- All tests pass: dev check
