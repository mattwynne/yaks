# Goal

Use example mapping to design and specify blocker edge cases before implementing the full matrix.

# Story

Prevent invalid blocker relationships when explicit blockers interact with hierarchy and moves.

As a yak user, I want blocker relationships and hierarchy moves to preserve an acyclic effective blocking graph, so that readiness remains understandable and yaks never deadlock each other.

# Example Mapping Method

Use four card types:

- Story: the behaviour slice under discussion
- Rules: concise business/domain rules
- Examples: concrete examples for each rule
- Questions: open questions or uncertainty

Keep examples concrete. If too many questions appear, split the story or create follow-up yaks.

# Rule: Effective blocker graph is directional and acyclic

Direction:

- `yx blocker add A --by B` means `B` blocks `A`, represented as edge `B → A`.
- hierarchy is an implicit dependency/blocker: child/descendant blocks parent/ancestor.
- explicit `BlockerAdded` events add explicit blocker edges.
- implicit hierarchy blockers and explicit blocker events together form the effective blocking graph.
- cycles are never allowed in the effective blocking graph.

# Rule: Adding an explicit blocker must not create a cycle

## Examples

- Self-blocking is rejected:
  - Given yak `A`
  - When `yx blocker add A --by A`
  - Then it fails and emits no `BlockerAdded` event.
- Mutual explicit cycle is rejected:
  - Given `A` is blocked by `B`
  - When `yx blocker add B --by A`
  - Then it fails and emits no `BlockerAdded` event.
- Longer explicit cycle is rejected:
  - Given `A` is blocked by `B`, and `B` is blocked by `C`
  - When `yx blocker add C --by A`
  - Then it fails and emits no `BlockerAdded` event.
- Hierarchy plus explicit cycle is rejected:
  - Given `child` is under `parent`
  - When `yx blocker add child --by parent`
  - Then it fails and emits no `BlockerAdded` event.
- Multi-ancestor hierarchy plus explicit cycle is rejected:
  - Given `grandchild` is under `child`, and `child` is under `parent`
  - When `yx blocker add grandchild --by parent`
  - Then it fails and emits no `BlockerAdded` event.
- Redundant hierarchy blocker is a no-op success:
  - Given `child` is under `parent`
  - When `yx blocker add parent --by child`
  - Then it succeeds, emits no event, and advises that `child` already blocks `parent` through hierarchy.

# Rule: Moving a yak must not make an explicit blocker relationship duplicate hierarchy

Moving changes hierarchy, which changes implicit blocking edges. A move must be rejected if it would make an active explicit blocker relationship become the same as an implicit hierarchy blocker.

This keeps the model simple: a dependency should be represented either by hierarchy or by an explicit blocker event, not both.

## Examples

- Existing hierarchy cycle remains rejected:
  - Given `child` is under `parent`
  - When `yx mv parent --under child`
  - Then it fails.
- Moving a blocker under the yak it blocks is rejected:
  - Given `A` is explicitly blocked by `B` (`B → A`)
  - When `yx mv B --under A`
  - Then it fails, because `B` would also block `A` through hierarchy.
  - And no `Moved` event is emitted.
- Moving a blocked yak under its blocker is rejected as a cycle:
  - Given `A` is explicitly blocked by `B` (`B → A`)
  - When `yx mv A --under B`
  - Then it fails, because hierarchy would add `A → B` and create a cycle.
  - And no `Moved` event is emitted.

# Example Mapping Topics

## Moves still to map

- moving subtrees with blockers pointing into the subtree
- moving subtrees with blockers pointing out of the subtree
- moving yaks such that an existing explicit blocker becomes an ancestor/descendant relationship indirectly
- ensuring existing “cannot move under descendant” protection still works

## Deletes / Prune

- removing a yak that blocks other yaks
- removing a yak that is blocked by other yaks
- pruning a done yak that used to be a blocker
- whether removal should emit `BlockerRemoved` events or whether projection should ignore relationships to removed yaks

## Duplicates / No-ops

- duplicate explicit add with no change: success, no event, advice
- duplicate explicit add with same reason: success, no event, advice
- duplicate explicit add with changed reason: `BlockerUpdated`
- explicit add redundant with hierarchy: success, no event, advice

# Expected Output

Before coding the full edge-case implementation, update this yak or create follow-up yaks with concrete Gherkin scenarios for the agreed behaviour.

At minimum, define which edge cases belong in the first vertical slice and which can be deferred safely.
