# Goal
Prevent agents from doing implementation work directly in the main repo.

# Decision
Simple rule: if `.git` is a directory (main repo, not a worktree), only allow:
- `read` (non-destructive)
- `dev *` / `bin/dev *` commands
- `yx *` commands

Everything else (`write`, `edit`, any other `bash`) is blocked.

Detection: worktrees have `.git` as a file (pointing to main repo's `.git/worktrees/`), while the main repo has `.git` as a directory.

# Done
- Restructured into `.pi/extensions/protect-main/` (directory with index.ts, rules.ts, rules.test.ts)
- 29 tests passing covering isMainRepo, isAllowedBashCommand, and decide logic
- Handles pipes: `echo "stuff" | yx context "foo"` is allowed because `yx` appears in the pipeline

# Blocked
- `dev start <yak>` command needs the yak ID for branch/worktree naming
- `yx show --format json` doesn't include the ID — need to fix that first
