# Goal
ShowLog builds Vec<LogEntryView> and calls display.show_log() once.

# Definition of Done
- ShowLog::execute builds Vec<LogEntryView> from events
- Relative time formatting done in use case (format_relative helper)
- Single call to app.display.show_log(&entries)
- Cucumber tests pass unchanged

# Known Knowns
- File: src/application/show_log.rs
- Uses format_relative for timestamps
- Each entry has narrative text, timestamp, event ID, commit SHA
