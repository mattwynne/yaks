# Status

Closed as superseded.

No `cliff.toml` is needed for the release flow that landed. Releases use a maintained `CHANGELOG.md` and helper scripts rather than `git-cliff`.

If automated commit-derived changelog generation becomes desirable later, reopen this as a fresh design.