After the gitignore onboarding prompt (or when running bare `yx` for the first time in a project), offer:

  "If you've never used yaks before, would you like a quick tutorial?" [Y/n]

Trigger: no project-level config file exists yet (e.g. .yaks/.config or similar marker). Once the user completes or declines the tutorial, write an "onboarding-complete" flag so it never asks again.

This should fire in two places:
1. At the end of the gitignore onboarding flow (ensure_gitignore.rs)
2. When running bare `yx` with no args in a project that has .yaks/ but no onboarding-complete marker

The tutorial itself should walk them through the basics interactively — add a yak, look at it, nest one under it, mark it done. Keep it short and hands-on.

Project-level config location TBD — could be .yaks/.config, a dotfile, or similar.