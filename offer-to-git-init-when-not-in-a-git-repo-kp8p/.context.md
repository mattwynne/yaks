# Status
Implementation exists in worktree/branch `offer-to-git-init-when-not-in-a-git-repo-kp8p`, but it is not done yet.

# Current problem
Merge/check failed. The branch offers `git init`, but full-stack tests fail because ordinary test repositories without `.yaks` in `.gitignore` now fail early with `.yaks folder is not gitignored`, causing widespread failures.

# Next step
Revisit the onboarding flow/test setup so the git-init offer works without breaking existing clean-repo scenarios.
