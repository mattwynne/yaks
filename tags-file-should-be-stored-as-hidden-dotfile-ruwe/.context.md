The `tags` field is stored as a plain file (`tags`) rather than a hidden dotfile (`.tags`), inconsistent with all other reserved fields. Need to:

1. Write ADR 0016 documenting the decision that all reserved fields must be stored as hidden dotfiles
2. Change the directory store to read/write `.tags` instead of `tags`
3. Add migration to rename existing `tags` files to `.tags`
4. Update any tests that reference the plain `tags` filename